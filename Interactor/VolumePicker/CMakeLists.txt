CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(VolumePicker)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(VolumePicker    main.cxx)
TARGET_LINK_LIBRARIES(VolumePicker ${VTK_LIBRARIES})