CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(SliderExample)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(SliderExample    slider.cxx)
TARGET_LINK_LIBRARIES(SliderExample  ${VTK_LIBRARIES})