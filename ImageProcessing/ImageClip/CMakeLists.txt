CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT( ImageClip )
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE( ImageClip  ImageClip.cpp)
TARGET_LINK_LIBRARIES(ImageClip ${VTK_LIBRARIES})
