CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(VisitImagePixel)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(VisitImagePixel	VisitImagePixel.cpp)
TARGET_LINK_LIBRARIES(VisitImagePixel ${VTK_LIBRARIES})
