CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(VolumeActor)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(VolumeActor VolumeActor.cpp)
TARGET_LINK_LIBRARIES(VolumeActor ${VTK_LIBRARIES})
