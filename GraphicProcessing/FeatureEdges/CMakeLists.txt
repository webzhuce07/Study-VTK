CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(FeatureEdges)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(FeatureEdges main.cpp)
TARGET_LINK_LIBRARIES(FeatureEdges ${VTK_LIBRARIES})

