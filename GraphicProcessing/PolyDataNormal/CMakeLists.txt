CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(PolyDataNormal)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(PolyDataNormal main.cpp)
TARGET_LINK_LIBRARIES(PolyDataNormal ${VTK_LIBRARIES})

