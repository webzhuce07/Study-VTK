CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(ClipPolyData)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(ClipPolyData main.cpp)
TARGET_LINK_LIBRARIES(ClipPolyData ${VTK_LIBRARIES})

