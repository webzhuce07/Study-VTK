CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(PolyDataDecimation)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(PolyDataDecimation PolyDataDecimation.cpp)
TARGET_LINK_LIBRARIES(PolyDataDecimation ${VTK_LIBRARIES})

