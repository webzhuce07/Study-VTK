CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(LinearExtrusion)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(LinearExtrusion main.cpp)
TARGET_LINK_LIBRARIES(LinearExtrusion ${VTK_LIBRARIES})

