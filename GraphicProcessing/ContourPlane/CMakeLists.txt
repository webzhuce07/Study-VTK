CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(ContourPlane)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(ContourPlane contourplane.cpp)
TARGET_LINK_LIBRARIES(ContourPlane ${VTK_LIBRARIES})

