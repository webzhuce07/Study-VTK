CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(PolyDataCurvature)
FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})
ADD_EXECUTABLE(PolyDataCurvature main.cpp)
TARGET_LINK_LIBRARIES(PolyDataCurvature ${VTK_LIBRARIES})

